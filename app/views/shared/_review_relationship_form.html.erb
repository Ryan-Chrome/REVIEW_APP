<% if user_signed_in? && current_user != @review.user %>
    <div id = "review_like_form">
        <% if current_user.review_like?(@review) %>
            <%= render 'shared/unlike' %>
        <% else %>
            <%= render 'shared/like' %>
        <% end %>
    </div>
<% end %>